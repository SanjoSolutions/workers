{
  "name": "worker",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "worker": "./build/bin/worker.js",
    "add-todo": "./build/bin/add-todo.js",
    "init-todo-repo": "./build/bin/init-todo-repo.js",
    "assistant": "./build/bin/assistant.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "prepare": "npm run build",
    "prepack": "npm run build",
    "test": "vitest run"
  },
  "dependencies": {
    "commander": "^14.0.3",
    "zx": "^8.8.5"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.21.0",
    "typescript": "^5",
    "vitest": "^4.0.18"
  }
}
